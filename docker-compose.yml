services:
  telegram-voice-crm:
    build: .
    container_name: telegram-voice-crm
    restart: unless-stopped
    env_file:
      - .env
    ports:
      - "100.67.75.111:3000:3000"
    volumes:
      - ./data:/app/data
      - ./uploads:/app/uploads
      - ./logs:/app/logs
